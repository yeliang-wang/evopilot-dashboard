{
  "name": "evopilot-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone EvoPilot dashboard for operating the EvoPilot API and CLI control plane.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run build && node tests/static-contract.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
